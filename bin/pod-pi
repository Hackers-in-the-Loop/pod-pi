#!/usr/bin/env python3
"""Run directly from a checkout or through an installed symlink."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from pod_pi.cli import main

if __name__ == "__main__":
    main()
